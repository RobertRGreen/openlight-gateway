import { UnimplementedDiscoveryProvider } from './provider.js';
// TODO: bounded inventory from an already configured account credential reference.
export class CloudAccountDiscoveryProvider extends UnimplementedDiscoveryProvider { readonly id = 'cloud-account'; }
