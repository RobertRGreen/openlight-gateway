import { UnimplementedDiscoveryProvider } from './provider.js';
// TODO: Tuya discovery with explicitly provisioned local credentials.
export class TuyaDiscoveryProvider extends UnimplementedDiscoveryProvider { readonly id = 'tuya'; }
